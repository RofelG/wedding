# Server
PORT=3000
ACCESS_CODE=LOVE2026

# Database client: sqlite (default) or mysql/mariadb
DB_CLIENT=sqlite

# SQLite
DB_PATH=./wedding.db

# MySQL / MariaDB (only used when DB_CLIENT=mysql or mariadb)
MYSQL_HOST=localhost
MYSQL_PORT=3306
MYSQL_USER=root
MYSQL_PASSWORD=changeme
MYSQL_DATABASE=wedding
MYSQL_POOL=10

# Branding
# Option A: single logo URL
LOGO_URL=https://example.com/path/to/logo.png
# Option B: light/dark logos built from base + type
LOGO_BASE=/img/logo
LOGO_TYPE=svg

