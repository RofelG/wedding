
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title>Admin Login</title>
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <meta name="robots" content="noindex,nofollow" />
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" />
  <link rel="stylesheet" href="/css/styles.css" />
  <style>
    body {
      background: radial-gradient(circle at 20% 20%, rgba(255,193,218,0.12), transparent 35%),
                  radial-gradient(circle at 80% 0%, rgba(133,150,255,0.12), transparent 30%),
                  #0f1116;
      color: #e6e6e6;
      min-height: 100vh;
    }
    .login-card {
      background: rgba(22, 26, 35, 0.9);
      border: 1px solid rgba(255,255,255,0.06);
      border-radius: 1rem;
      box-shadow: 0 20px 60px rgba(0,0,0,0.35);
      backdrop-filter: blur(6px);
    }
    .login-card .form-control {
      background: rgba(255,255,255,0.05);
      border: 1px solid rgba(255,255,255,0.12);
      color: #f8f9fa;
    }
    .login-card .form-control:focus {
      border-color: #ff6f91;
      box-shadow: 0 0 0 0.2rem rgba(255,111,145,0.2);
    }
    .brand-mark {
      width: 48px;
      height: 48px;
      border-radius: 12px;
      background: linear-gradient(135deg, #ff6f91, #ff92c3);
      display: inline-flex;
      align-items: center;
      justify-content: center;
      color: #0f1116;
      font-weight: 700;
      font-size: 1.2rem;
    }
  </style>
</head>
<body>
  <div class="container d-flex align-items-center justify-content-center" style="min-height: 100vh;">
    <div class="login-card p-4 p-md-5" style="max-width: 420px; width: 100%;">
      <div class="d-flex align-items-center gap-3 mb-3">
        <div class="brand-mark">RJ</div>
        <div>
          <p class="text-uppercase text-muted small mb-1">Wedding Admin</p>
          <h1 class="h5 mb-0 text-white">Sign in</h1>
        </div>
      </div>
      <% if (typeof error !== "undefined" && error) { %>
        <div class="alert alert-danger py-2 mb-3"><%= error %></div>
      <% } %>
      <form method="POST" action="/admin/login" class="d-grid gap-3">
        <div>
          <label for="username" class="form-label text-muted small">Username</label>
          <input type="text" class="form-control" id="username" name="username" placeholder="admin" required />
        </div>
        <div>
          <label for="password" class="form-label text-muted small">Password</label>
          <input type="password" class="form-control" id="password" name="password" placeholder="••••••••" required />
        </div>
        <button type="submit" class="btn btn-wedding-primary w-100">Sign in</button>
        <a href="/" class="text-center text-muted small text-decoration-none">Back to site</a>
      </form>
    </div>
  </div>
</body>
</html>
