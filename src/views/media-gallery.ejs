
<!DOCTYPE html>
<html lang="en">
<head>

  <!-- Google tag (gtag.js) -->
  <script async src="https://www.googletagmanager.com/gtag/js?id=<%= process.env.GA_TRACKING_ID %>"></script>
  <script>
    window.dataLayer = window.dataLayer || [];
    function gtag(){dataLayer.push(arguments);}
    gtag('js', new Date());

    gtag('config', '<%= process.env.GA_TRACKING_ID %>');
  </script>

  <meta charset="UTF-8" />
  <title>Wedding Uploads</title>
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <meta name="robots" content="noindex,nofollow" />
  <link
    href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css"
    rel="stylesheet"
  />
  <link rel="stylesheet" href="/css/styles.css" />
  <style>
    .masonry-grid {
      column-count: 1;
      column-gap: 1rem;
    }
    @media (min-width: 576px) {
      .masonry-grid { column-count: 2; }
    }
    @media (min-width: 992px) {
      .masonry-grid { column-count: 3; }
    }
    @media (min-width: 1200px) {
      .masonry-grid { column-count: 4; }
    }
    .masonry-item {
      display: inline-block;
      width: 100%;
      margin: 0 0 1rem;
      break-inside: avoid;
      cursor: pointer;
    }
    .masonry-item img {
      width: 100%;
      height: auto;
      border-radius: 0.75rem;
      box-shadow: 0 6px 18px rgba(0,0,0,0.08);
    }
    .masonry-item small {
      display: block;
      margin-top: 0.35rem;
      color: #6c757d;
      overflow: hidden;
      text-overflow: ellipsis;
      white-space: nowrap;
    }
  </style>
</head>
<body class="rsvp-page">
  <%- include('partials/nav', { navLinks, brandHref: '/' }) %>

  <div class="container py-5" style="margin-top: 7rem;">
    <div class="d-flex justify-content-between align-items-center mb-4">
      <div>
        <p class="text-uppercase text-muted small mb-1">Rofel & Julie Ann</p>
        <h1 class="h4 mb-0">Uploaded Photos</h1>
      </div>
      <div class="d-flex gap-2">
        <a class="btn btn-outline-secondary btn-sm" href="/media">Upload more</a>
      </div>
    </div>

    <div id="galleryGrid" class="masonry-grid"></div>
    <div id="galleryStatus" class="text-center text-muted py-3"></div>
  </div>

  <!-- Lightbox -->
  <div class="modal fade" id="lightboxModal" tabindex="-1" aria-hidden="true">
    <div class="modal-dialog modal-dialog-centered modal-lg">
      <div class="modal-content bg-dark text-white">
        <div class="modal-header border-0">
          <h5 class="modal-title">Photo</h5>
          <button type="button" class="btn-close btn-close-white" data-bs-dismiss="modal" aria-label="Close"></button>
        </div>
        <div class="modal-body text-center">
          <img id="lightboxImage" src="" alt="Full size" class="img-fluid" />
        </div>
      </div>
    </div>
  </div>

  <%- include('partials/footer') %>
  <script src="/js/media-gallery.js"></script>
</body>
</html>
